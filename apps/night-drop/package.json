{
  "name": "@hustle/night-drop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@hustle/core": "0.1.0",
    "@hustle/routerun": "0.1.0",
    "three": "^0.185.1"
  },
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "vite build",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "assets:validate": "node tools/blender/validate_night_drop_runner_assets.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.185.1"
  }
}
